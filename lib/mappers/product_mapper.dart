import 'package:emarsys_sdk/model/predict/product.dart';

import 'mapper.dart';

class ProductMapper extends Mapper<List<Product>, List<dynamic>> {
  @override
  List<Product> map(List input) {
    return [];
  }
}
